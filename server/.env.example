PORT=5000
MONGO_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/DATABASE
SMTP_PASS=
SMTP_USER=
SENDER_EMAIL=
JWT_SECRET=

# OpenAI — design concept / vision analysis (https://platform.openai.com/api-keys)
OPENAI_API_KEY=
# Optional override (default gpt-4o-mini)
# OPENAI_DESIGN_MODEL=gpt-4o-mini
# Image previews (DALL·E 3 — billed per image)
# OPENAI_IMAGE_MODEL=dall-e-3

# Optional — HTML emails & subjects
EMAIL_BRAND_NAME=Build-A-Job
# Optional — shown in email footer
SUPPORT_EMAIL=
